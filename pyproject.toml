[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "claude-cli-toolkit"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["anthropic>=0.52.0"]

[project.scripts]
claude-toolkit = "cli_tool:main"
